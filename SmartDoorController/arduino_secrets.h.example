#pragma once

// Copy this file to arduino_secrets.h in the same sketch folder, then replace
// the example values. The real arduino_secrets.h file is ignored by Git so
// your home Wi-Fi password is not uploaded to GitHub.
#define SMARTDOOR_WIFI_SSID "YOUR_2_4_GHZ_WIFI_NAME"
#define SMARTDOOR_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
